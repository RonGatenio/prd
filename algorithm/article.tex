\documentclass[nonacm,sigplan]{acmart}
% \documentclass[a4paper,12pt]{article}

% includes
\input{macros.tex}

% packages
\usepackage{lipsum}

% enable page numbers
\settopmatter{printfolios=true}

% make references clickable 
\usepackage[]{hyperref}

\begin{document}

\title{Concurrent Persistency Race Bug Detector}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Authors
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% TODO: removed for anonymity

\author{Ron Gatenio}
\affiliation{%
  \institution{Technion}
  \city{Haifa}
  \country{Israel}
}
\email{ron.gatenio@campus.technion.ac.il}

\author{Erez Petrank}
\affiliation{%
  \institution{Technion}
  \city{Haifa}
  \country{Israel}
}
\email{erez@cs.technion.ac.il}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Abstract
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}

The introduction of new NVM technologies with byte addressable access and DRAM performance has increased interest in developing crash-consistent software.
This type of software introduces a new type of inconsistency bugs between the volatile CPU cache and the persistent NVM state.

This research introduces CPRD, a concurrent NVM bug detector that analyzes the program trace using a graph search algorithm.

\end{abstract}

\maketitle % should come after the abstract
\pagestyle{plain} % should come right after \maketitle


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Introduction
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}

\iffalse
TODO:
Background
This paper presents CPRD, ...
The sections are ...
\fi

Nonvolatile memory (NVM) technologies, such as Intel’s Optane DC Persistent Memory \cite{intel2019optane}, offer both persistence similar to disks with performance close to that of DRAM. NVM storage is accessed, like DRAM, with load and store instructions. This provides new opportunities to design crash-consistent software without high storage overhead. However, designing and implementing such programs is difficult. NVM data on a volatile cache may not persist after a crash, and it may also evict cache lines in an arbitrary order, which may differ from the program’s store order.

To ensure consistency of NVM state, developers need to explicitly evict cache lines to the NVM. This can be done with explicit flush (e.g., CLWB) and fence (e.g., SFENCE) instructions \cite{intel2020abi}. Due to the need to ensure correct cache line evictions, even experienced programmers may introduce inconsistency bugs, if they are not experienced with PM programming practices. This introduces a need for special bug detectors that could help developers find such complicated bugs. Previous bug detectors, aimed at detecting data inconsistencies, won’t necessarily be able to find NVM related bugs. This type of programming model that uses byte addressable persistent memory did not exist before. 

In this work, we focus on a specific type of bug that can be introduced in concurrent NVM programs. Previously coined as \pmracerace \cite{pmrace2022}, a bug that occurs when an NVM location affects data written to another NVM location before being persisted. Such bug can cause an inconsistent NVM state after a crash if the affected written address got persisted before the read address. In the following, we refer to this bug as \race.

Previous work \cite{pmrace2022} attempted to detect the bug using a smart fuzzing technique to explore the inter-thread interleaving of the program.
We attempt to solve the interleaving exploration using a vector clock analysis that detects happens-before relations between program events \cite{lamport1978}.
To do this, we use LLVM's ThreadSanitizer \cite{tsan2009}.
In addition, we want to detect all write events to persistent memory that are affected by a read event from persistent memory. We will use data-flow analysis to determine whether a read event affects a write event later in the execution.
For this, we will use LLVM's DataFlowSanitizer \cite{dfsan2020}.

Using both the happens-before analysis and the data-flow analysis we can construct 2 graphs that represent the relations between all execution events.
A \HBGName, denoted as $\HBG$, and a \PDGName \cite{dpdg1991} denoted as $\PDG$
\myrace detector will use the 2 graphs to traverse the program execution trace and report all discovered bugs using the algorithm presented later in \autoref{sec:algorithm}.


% ChatGPT:
% Concurrent Persistency Race Detector (CPRD) is a novel tool designed to identify and analyze concurrent persistency races in applications leveraging persistent memory, such as Intel's Optane DC Persistent Memory. With the advent of non-volatile memory technologies, ensuring the correctness of persistent data structures in the face of concurrent accesses and system failures has become paramount. CPRD addresses this challenge by instrumenting the target application, generating a detailed execution trace that includes memory access, synchronization events, and flush operations to persistent memory. By constructing and analyzing a Happens-Before graph (HBG) from the execution trace, CPRD effectively identifies potential data races that could lead to inconsistencies in persistent memory, thus aiding developers in the design of robust and error-free persistent memory applications.

% ChatGPT:
% This document presents an algorithm designed to identify and mitigate Concurrent Persistence Races (CPR) in multithreaded applications. At its core, the algorithm instruments a given program to trace execution, thereby generating a detailed log of memory accesses, synchronization events, and flush operations. Utilizing this trace, we construct a Happens-Before Graph (HBG) and a Program Dependency Graph (PDG), fundamental structures for understanding the temporal and dependency relations between different program events.
% Our approach innovates on existing methods by offering a more precise detection of potential persistence races, leveraging advanced vector clock techniques to account for the nuanced interplay of read, write, and flush events across threads. The ultimate goal is to offer developers a tool for preemptively identifying race conditions that could lead to inconsistent or erroneous program states in persistent memory environments. This document details the algorithm's theoretical foundation, practical implementation, and expected outcomes, aiming to provide a comprehensive framework for enhancing program reliability and performance in concurrent systems.


% TODO: write about other sections
% TODO: conclude the intro


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Background and Preliminaries
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% \section{Preliminaries}
\section{Background}
% \section{Background and Preliminaries}

\subsection{Persistent Memory Programming}
% TODO: write

\subsection{Program Instrumentation}
Program instrumentation allows us to record runtime events that occur during program execution.
Using LLVM's IR instrumentation %TODO: cite?
we will record every PM access (reads and writes) and every flush/fence instructions.
In addition, we will use LLVM's ThreadSanitizer to obtain happens-before edges between recorded instructions, and LLVM's DataFlow Sanitizer to obtain program dependency relations between execution events in runtime.

% TODO: rewrite

\myparagraph{Happens-Before Analysis}
LLVM's ThreadSanitizer \cite{tsan2009}, originally developed by Google, is a data race detection tool that instruments program execution and reports data races, using a vector clock analysis \cite{lamport1978}.
In Lamport's original paper, vector clocks are updated when a process sends a message to another process. In the case of ThreadSanitizer, the messages are considered to be any event in one thread that is guarantied to happen before another event in another thread.
Events like thread creation, join operations, lock acquisitions and releases, and use of atomic primitives like InterlockIncrement, InterlockXchg etc.

We utilize ThreadSanitizer's precise instrumentation and vector clock updates in order to record happens-before edges between program events.

% TODO: rewrite

\myparagraph{Dynamic Program Dependence Analysis}
LLVM's Data-Flow Sanitizer \cite{dfsan2020} is a tool used to detect the effects of program events on one another

% TODO: complete

\subsection{\race}

% TODO: write


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Algorithm
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Algorithm}
\label{sec:algorithm}

This section describes the algorithm used to detect {\cprd}s. We first formalize the definition of a \cprd and then detail the algorithmic steps for its detection.


% ---------------------------------------------------------
% Preliminaries
% ---------------------------------------------------------
\subsection{Preliminaries}
The program trace categorizes events into three main types: event nodes $\Nodes$, happens-before edges $\HBEdges$, and program dependency edges $\PDEdges$. We denote the subsets of Read, Write, and Flush event nodes as $\ReadNodes$, $\WriteNodes$, and $\FlushNodes$, respectively. Each event node $\NodeV$ is associated with a variable $\var{\NodeV} \in \Vars$ indicating the memory address accessed, and a thread $\thread{\NodeV} \in \Threads$ indicating the executing thread. Each event node is also sequentially identified by $\idx{\NodeV} \in \IDs$ within its thread, reflecting the execution order.


% ---------------------------------------------------------
% Graphical Representation
% ---------------------------------------------------------
\subsection{Graphical Representation}
% We represent the execution trace as two directed acyclic graphs:
The execution trace is modeled using two directed acyclic graphs (DAGs):

% ~~~~~~~~~~~~~~~~~~~~~~~
% HBG
% ~~~~~~~~~~~~~~~~~~~~~~~
\myparagraph{\HBGName}

% TODO: the first paragraph could go to the background section

Lamport’s happens-before graph (HBG) \cite{lamport1978} is a theoretical construct used to model the execution dynamics of concurrent programs. Within this graph, every executed instruction is represented as a node, and directed edges between these nodes create a partial-order that outlines the execution order between instructions.
This abstraction is particularly useful for analyzing concurrent executions \cite{chess2008}, as it decouples the execution order from the actual timing of instructions. Consequently, different executions that may run at varying speeds but share the same happens-before graph are considered behaviorally equivalent, reflecting consistent execution behavior despite temporal differences.
% Previous works have also modeled program execution in this way \cite{chess2008}.

We abstract the program execution with a happens-before graph and denote it as $\HBG = (\Nodes, \HBEdges)$. A directed path from node $\NodeE_1$ to node $\NodeE_2$ implies that the event at $\NodeE_1$ happens-before the event at $\NodeE_2$, establishing a precedence relation.
The graph structures the nodes to reflect the execution of the program threads. Nodes within the same thread are linked by intra-thread edges, illustrating sequential execution steps, while nodes across different threads are linked by inter-thread edges, demonstrating the happens-before relationship between them.


% ~~~~~~~~~~~~~~~~~~~~~~~
% PDG
% ~~~~~~~~~~~~~~~~~~~~~~~
\myparagraph{\PDGName}

% TODO: choose symbols
\newcommand{\tempNodeOne}{\ensuremath{\NodeE_1}}
\newcommand{\tempNodeTwo}{\ensuremath{\NodeE_2}}
\renewcommand{\tempNodeOne}{\ensuremath{\NodeWEx}}
\renewcommand{\tempNodeTwo}{\ensuremath{\NodeR}}

% TODO: choose who to site. the definition here is from pd1992. \cite{pd1988} \cite{pd1990} \cite{pd1991} \cite{pd1992} \cite{pd1994book}
The Dynamic Dependency Graph (DDG) \cite{pd1988} is a directed, acyclic graph that models the execution flow of a program as a partial order. In the DDG, each node corresponds to an executed instruction, and directed edges illustrate the dependencies that exist between these instructions.
Dependencies in a DDG can be classified into several types \cite{pd1992}, primarily including data dependencies and control dependencies. A data dependency arises between two operations when the output of one operation (e.g., a value produced) is required as input for the other operation. Conversely, a control dependency exists when the execution path of one operation depends on the outcome of a conditional branch affecting another operation.

We denote the DDG as $\PDG = (\Nodes, \PDEdges)$. Within this graph, an edge from $\tempNodeOne$ to $\tempNodeTwo$ indicates that the execution of $\tempNodeOne$ relies on the result of $\tempNodeTwo$.
Our analysis particularly focuses on dependencies in dynamic executions where a read event $\tempNodeTwo$ precedes the first immediate write event $\tempNodeOne$ within the same thread but on a different cache line. The rationale behind this selective focus will be elaborated upon later.
% Specifically, in our trace analysis, we restrict $\PDEdges$ to include only dependencies where a read event $\tempNodeTwo$ is followed by the first subsequent write event $\tempNodeOne$ within the same thread and on a different cache line. We will later explain the reasoning for this choice.


% TODO: PDG: where to explain about the structure of the PDG and that each read has only one write... This explnation requires an understanding of the race (to understand why we did this)

% TODO: explain that the \racerange for the earliest write is the largest...

% TODO: add image of both graphs with threads etc.


% ~~~~~~~~~~~~~~~~~~~~~~~
% Vector Clocks
% ~~~~~~~~~~~~~~~~~~~~~~~
\subsection{State Vector Clocks}
Vector clocks, building upon Lamport's timestamps \cite{lamport1978} and further developed in \cite{fidge1987vc} and \cite{mattern1988vc}, are a widely used mechanism for capturing causality among events in parallel and distributed systems. 
This tool is used to determine the happens-before partial-order of the executed program events.

We use vector clocks in many ways thought this work. Firstly, they are used by ThreadSanitizer \cite{tsan2009} to determine the happens-before relations in the program's execution. Secondly, use the same concept in our \HBGName analysis to determine different states of persistency and ordering of events in the program execution trace.

In our analysis we define vector clocks for each event node in the execution.
We use several types of vector clocks in order to keep track of different states during the execution trace. In general, each vector clock that we define is a $\Threads$ sized vector of event IDs (used like logical clocks) where each index $\Thread$ indicates a some known state from thread $\Thread$.

\myparagraph{Happens-Before Vector Clock}
The Happens-Before Vector Clock ($\HBVC{}{}$) is defined for each node $\NodeV \in \Nodes$ as the function $\HBVC{\NodeV}{\Thread,\VarX}: \HBVCDef$. This function determines the index of the latest event in thread $\Thread$ that precedes $\NodeV$ in $\HBG$ and affects the cache line associated with $\VarX$.
An event $\NodeU$ is considered to \textit{happen-before} another event $\NodeV$ if $\idx{\NodeU} \leq \HBVC{\NodeV}{\thread{\NodeU},\var{\NodeU}}$, establishing a causal order between these events.

\myparagraph{Persisted-Before Vector Clock}
The Persisted-Before Vector Clock ($\PBVC{}{}$) is defined for each node $\NodeV \in \Nodes$ as the function $\PBVC{\NodeV}{\Thread,\VarX}: \PBVCDef$. This function determines the index of the latest event in thread $\Thread$ that precedes $\NodeV$ in $\HBG$, affects the cache line associated with $\VarX$, and ensures that $\VarX$ has been persisted.
An event $\NodeU$ is considered \textit{persisted-before} another event $\NodeV$ if $\idx{\NodeU} \leq \PBVC{\NodeV}{\thread{\NodeU},\var{\NodeU}}$.
Persistency here implies that there is a path in the $\HBG$ from $\NodeU$ to $\NodeV$ that contains a Flush event on the cache line associated with $\VarX$.

\myparagraph{Happens-After Vector Clock}
The Happens-After Vector Clock ($\HAVC{}{}$) is defined for each node $\NodeV \in \Nodes$ as the function $\HAVC{\NodeV}{\Thread}: \HAVCDef$. This function determines the index of the earliest event in thread $\Thread$ that happens-after $\NodeV$ in $\HBG$.
An event $\NodeU$ is considered to \textit{happen-after} another event $\NodeV$ if $\HAVC{\NodeV}{\thread{\NodeU}} \leq \idx{\NodeU}$, establishing a causal order between these events.

% TODO: should we use the word epoch instead of index everywhere?

\section{STOP}
\section{STOP}
\section{STOP}
\section{STOP}












\myparagraph{Happens-Before State}
\myparagraph{Happens-After State}



To evaluate the persistence state of memory location $\VarX$ at any point during execution, for each node $\NodeV \in \Nodes$ we introduce the function $\PBVC{\NodeV}{\Thread,\VarX}$ that determines the index of the last node in thread $\Thread$ concerning variable $\VarX$ that was persisted prior to node $\NodeV$. This implies the node occurred before a flush event targeting the same cache line.


In addition, for each event node we introduce the function $\HAVC{\NodeV}{\Thread}$ which identifies the index of the first node in thread $\Thread$ that happen-after node $\NodeV$.


We will define 

To assess the persistence status of a memory location $\VarX$ during execution, we define:
We will define


\subsection{\race Definition}

A \race occurs when a write operation to persistent memory (PM) is influenced by a prior read operation from a non-persisted area of PM (on a different cache line). If the write operation becomes durable before the read operation, the PM state could be compromised if program recovery process won't fix the problem.

\paragraph{Persistence Criteria} The memory location $\VarX$, involved in the read operation $\NodeR$, is considered \emph{persisted} at the time of a subsequent write event $\NodeWEx$ if one of the following is true:
\begin{itemize}
    \item There exists a flush event that happens before $\NodeWEx$ and after $\NodeR$, ensuring that the effects of $\NodeR$ are committed to PM before $\NodeWEx$ executes, or
    \item After every write operation to $\VarX$ that does not happen-after $\NodeR$ (therefor it could influence the read), there is a flush event preceding $\NodeWEx$. This guarantees that all previous modifications of $\VarX$ persist before $\NodeWEx$.
\end{itemize}

\begin{definition}[\race]
    \label{def:race}
    A \textbf{\race} occurs between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ when both nodes access overlapping memory locations, and there is no direct causal (happens-before) relationship from $\NodeR$ to $\NodeW$. For such a race to be identified, there must exist another write node $\NodeWEx \in \WriteNodes$ that is program-dependent on $\NodeR$, resides within the same thread but operates on a different cache line. Additionally, this relationship is only valid if no flush event $\NodeF$, which would guarantee the persistence of the data involved before $\NodeWEx$, occurs after $\NodeR$ or $\NodeW$ and before $\NodeWEx$.
\end{definition}


% \begin{definition}[\race]
%     A \textbf{\race} between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ is defined if all of the following conditions are met:
%     \begin{enumerate}
%         \item $\NodeR$ and $\NodeW$ operate on overlapping or the same memory locations.
%         \item There is no happens-before relationship from $\NodeR$ to $\NodeW$; i.e., $\NodeW$ precedes $\NodeR$ or are concurrent.
%         \item There exists a write node $\NodeWEx \in \WriteNodes$ such that:
%             \begin{itemize}
%                 \item $\NodeWEx$ is program-dependent on $\NodeR$,
%                 \item $\NodeWEx$ and $\NodeR$ are executed within the same thread,
%                 \item $\NodeWEx$ operates on a different cache line than that of $\NodeR$.
%                 \item There is no flush event $\NodeF \in \FlushNodes$ that occurs between $\NodeR$ and $\NodeWEx$ which could persist the state of the memory location modified by $\NodeR$ or any overlapping write operation $\NodeW$.
%             \end{itemize}
%     \end{enumerate}
% \end{definition}




\section{STOP}
\section{STOP}
\section{STOP}
\section{STOP}















A \race emerges when a modification of persistent memory (PM) is influenced by a read operation from an area in PM that has not yet persisted (and is located on different cache lines).
To detect such race, we assess the persistence status of the memory address $\VarX$ involved in the read operation at the time the corresponding PM modification is executed.

A \race occurs when two operations on overlapping memory locations could lead to inconsistent or non-durable data states in persistent memory (PM). Specifically, a CPR involves a scenario where a read operation may influence subsequent write operations due to lack of intervening persistency guarantees. 


\paragraph{Persistence Criteria} The memory location $\VarX$, involved in the read operation $\NodeR$, is considered \emph{persisted} at the time of a subsequent write event $\NodeWEx$ if one of the following is true:
\begin{itemize}
    \item There exists a flush event that happens before $\NodeWEx$ and after $\NodeR$, ensuring that the effects of $\NodeR$ are committed to PM before $\NodeWEx$ executes, or
    \item After every write operation to $\VarX$ that does not happen-after $\NodeR$, there is a flush event preceding $\NodeWEx$. This guarantees that all previous modifications of $\VarX$ persist before $\NodeWEx$.
\end{itemize}



\subsection{TEMP}

\subsection{Definition of Concurrent Persistency Race}

A \textbf{Concurrent Persistency Race (CPR)} occurs when a write operation to persistent memory (PM) is potentially influenced by a preceding read operation from a region in PM that has not been persisted yet. This scenario typically involves interactions across different cache lines, which may compromise data consistency and durability.

\paragraph{Operational Conditions} Consider a memory address $\VarX$ accessed by a read operation $\NodeR$. A \textbf{CPR} is said to emerge under the following conditions:
\begin{itemize}
    \item $\NodeR$ reads from $\VarX$ which resides in a non-persisted state within PM.
    \item There exists a subsequent write operation $\NodeWEx$ to $\VarX$, occurring on a different cache line than the one accessed by $\NodeR$.
    \item The persistency status of $\VarX$ is influenced by the read operation $\NodeR$ at the time when $\NodeWEx$ is executed, implying that changes made to $\VarX$ by $\NodeWEx$ could be inconsistent with earlier operations due to the lack of persistence.
\end{itemize}

To detect such races, we assess whether $\VarX$ was persisted after being read by $\NodeR$ and before being modified by $\NodeWEx$. Specifically, $\VarX$ is considered persisted at the moment of the write operation $\NodeWEx$ if:
\begin{itemize}
    \item A flush event targeted at $\VarX$ occurs after $\NodeR$ and before $\NodeWEx$, ensuring any data read by $\NodeR$ and any intermediate modifications are committed to PM prior to $\NodeWEx$.
    \item For every write operation to $\VarX$ that does not sequentially follow the read operation $\NodeR$ within the same execution context, a corresponding flush event must precede $\NodeWEx$, confirming that all preceding changes to $\VarX$ are durable before $\NodeWEx$ is executed.
\end{itemize}

This definition encapsulates the key elements of persistency races involving non-volatile memory, highlighting the crucial role of appropriate synchronization and flush operations in maintaining data consistency across concurrent accesses.
















\subsection{Concurrent Persistency Race Definition}

A \textbf{Concurrent Persistency Race (CPR)} occurs when a write operation to persistent memory (PM) is influenced by a prior read operation from a non-persisted area of PM. Specifically, this race condition arises when the read and write operations interact across different cache lines, potentially leading to inconsistencies if the read data is not yet durable.

\paragraph{Persistence Criteria} The memory location $\VarX$, involved in the read operation $\NodeR$, is considered \emph{persisted} at the time of a subsequent write event $\NodeWEx$ if:
\begin{itemize}
    \item There exists a flush event that happens before $\NodeWEx$ and after $\NodeR$, ensuring that the effects of $\NodeR$ are committed to PM before $\NodeWEx$ executes, or
    \item For every write operation to $\VarX$ that does not sequentially follow $\NodeR$ within the same thread, there is a flush event preceding $\NodeWEx$. This guarantees that all preceding modifications to $\VarX$ are persisted before $\NodeWEx$.
\end{itemize}

These conditions ensure that $\VarX$ is in a persisted state by the time $\NodeWEx$ occurs, having been flushed following any influencing operations.

\begin{definition}[Concurrent Persistency Race]
    A \textbf{Concurrent Persistency Race} between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ is defined if all of the following conditions are met:
    \begin{enumerate}
        \item $\NodeR$ and $\NodeW$ operate on overlapping or the same memory locations.
        \item There is no happens-before relationship from $\NodeR$ to $\NodeW$; i.e., $\NodeR$ and $\NodeW$ are concurrent.
        \item There exists a write node $\NodeWEx \in \WriteNodes$ such that:
            \begin{itemize}
                \item $\NodeWEx$ is program-dependent on $\NodeR$,
                \item $\NodeWEx$ and $\NodeR$ are executed within the same thread,
                \item $\NodeWEx$ operates on a different cache line than that of $\NodeR$.
            \end{itemize}
        \item There is no flush event $\NodeF$ that occurs between $\NodeR$ and $\NodeWEx$ which could persist the state of the memory location modified by $\NodeR$ or any overlapping write operation $\NodeW$.
    \end{enumerate}
\end{definition}

This definition encapsulates the essential elements of a persistency race in systems using non-volatile memory, highlighting the critical role of flush operations in maintaining data consistency across concurrent accesses.



























\subsection{Definition of Concurrent Persistency Race}

A Concurrent Persistency Race (CPR) occurs when two operations on overlapping memory locations could lead to inconsistent or non-durable data states in persistent memory (PM). Specifically, a CPR involves a scenario where a read operation may influence subsequent write operations due to lack of intervening persistency guarantees. 

\begin{definition}[Concurrent Persistency Race]
    A \textbf{Concurrent Persistency Race} between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ is said to occur if the following conditions are met:
    \begin{enumerate}
        \item Both nodes $\NodeR$ and $\NodeW$ operate on the same memory location or overlapping memory regions.
        \item There is no direct happens-before relationship from $\NodeR$ to $\NodeW$, i.e., $\NodeR$ and $\NodeW$ are concurrent.
        \item There exists a write node $\NodeWEx$ that:
            \begin{itemize}
                \item Is program-dependent on $\NodeR$,
                \item Resides in the same thread as $\NodeR$,
                \item Operates on a different cache line than $\NodeR$,
            \end{itemize}
        \item No flush operation $\NodeF$ happens-before $\NodeWEx$ that could persist the effects of any write operation overlapping with $\NodeR$ or $\NodeW$. This includes flush operations that:
            \begin{itemize}
                \item Happen after $\NodeR$ and before $\NodeWEx$,
                \item Ensure the persistency of the data accessed by $\NodeW$ before the execution of $\NodeWEx$.
            \end{itemize}
    \end{enumerate}
    These conditions collectively ensure that the write operation $\NodeW$ could be influenced by the read operation $\NodeR$ under a concurrent execution scenario, leading to a persistency race if not adequately managed by intervening flush operations.
\end{definition}

The implications of such races are critical in systems utilizing non-volatile memory technologies, where the durability and consistency of memory states are paramount. This definition forms the basis for the algorithmic detection of CPRs, as elaborated in the subsequent sections.










A \race emerges when a modification of persistent memory (PM) is influenced by a read operation from an area in PM that has not yet persisted (and is located on different cache lines).
To detect such race, we assess the persistence status of the memory address $\VarX$ involved in the read operation at the time the corresponding PM modification is executed. 

$\VarX$ is considered persisted at the moment of the event node $\NodeWEx$ if it meets one of the following conditions: A Flush event precedes $\NodeWEx$ and occurs after the read event $\NodeR$ that influences it, or, for every write event to $\VarX$ not succeeding the read event $\NodeR$, there exists a Flush event that precedes $\NodeWEx$. This ensures that $\VarX$ is in a persisted state by the time $\NodeWEx$ takes place, having been flushed following the influencing read operation or write operations that might have affected the read.
% We say that $\VarX$ is persisted at the time of the event node $\NodeWEx$ if a Flush event happens-before $\NodeWEx$ and happen-after the influencing read event $\NodeR$ or after all write events to $\VarX$ that don't happen-after the read event $\NodeR$, there is a flush event that happens-before the $\NodeWEx$ event.

\begin{definition}
    \label{def:race}
    We define a \race between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ if both operate on overlapping memory locations, $\NodeR$ does not happens-before $\NodeW$, and $\exists \NodeWEx \in \WriteNodes$ such that $\NodeWEx$ is program-dependent on $\NodeR$ (in the same thread and operates on a different cache line), and there is no flush event $\NodeF$ that happens-before $\NodeWEx$ such that $\NodeF$ happen-after $\NodeR$ or happen-after $\NodeW$.
\end{definition}

% In order to inquire about the persistency state of memory location $\VarX$ at any given point in the execution, we will define two functions for each event node $\NodeV \in \Nodes$: $\HBVC{\NodeV}{\Thread,\VarX}$, the last node's index in thread $\Thread$ of variable $\VarX$ that happens-before the node $\NodeV$, and $\PBVC{\NodeV}{\Thread,\VarX}$, the last node's index in thread $\Thread$ of variable $\VarX$ that was persisted before the node $\NodeV$, meaning that it  happens-before a flush event (on the same cache line).

% To evaluate the persistence status of memory location $\VarX$ at any point during execution, we introduce two functions for each event node $\NodeV \in \Nodes$. The function $\HBVC{\NodeV}{\Thread,\VarX}$ identifies the index of the last node in thread $\Thread$ concerning variable $\VarX$ that precedes node $\NodeV$. The function $\PBVC{\NodeV}{\Thread,\VarX}$ determines the index of the last node in thread $\Thread$ concerning variable $\VarX$ that was persisted prior to node $\NodeV$. This implies the node occurred before a flush event targeting the same cache line.
To evaluate the persistence status of memory location $\VarX$ at any point during execution, for each node we $\NodeV \in \Nodes$ introduce the function $\PBVC{\NodeV}{\Thread,\VarX}$ that determines the index of the last node in thread $\Thread$ concerning variable $\VarX$ that was persisted prior to node $\NodeV$. This implies the node occurred before a flush event targeting the same cache line.
In addition, for each event node we introduce the function $\HAVC{\NodeV}{\Thread}$ which identifies the index of the first node in thread $\Thread$ that happen-after node $\NodeV$.

Using these functions, we will redefine \cprd as follows.
\begin{definition}
    \label{def:race2}
    We define a \race between a read node $\NodeR \in \ReadNodes$ and a write node $\NodeW \in \WriteNodes$ if both operate on overlapping memory locations, and $\exists \NodeWEx \in \WriteNodes$ such that $\NodeWEx$ is program-dependent on $\NodeR$ (in the same thread and operates on a different cache line), and 
    $$\PBVC{\NodeWEx}{\thread{\NodeW}, \var{\NodeW}} < \idx{\NodeW} < \HAVC{\NodeR}{\thread{\NodeW}}$$
    and
    $$\PBVC{\NodeWEx}{\thread{\NodeWEx}, \var{\NodeR}} < \idx{\NodeR}$$
\end{definition}

% TODO: show interval image
Essentially, if the \readnode isn't persisted, we define an interval in each thread between the persistency point of $\VarX$ and the point that happen-after the read (meaning it has no affect on the read value). We call this interval \racerange, and it is defined for each write node and for each thread.
% TODO: not explained great



\subsection{Algorithm}
The algorithm is comprised of three main stages: in \autoref{alg:havc} we construct the $\HAVC{}{}$ for each node, in \autoref{alg:pbvc} we construct $\PBVC{}{}$ for each node, and in \autoref{alg:cprd}, we identify {\cprd}s based on the two functions.


% ---------------------------------------------------------
% Algorithm - 1. HAVC
% ---------------------------------------------------------

\myparagraph{Happens-After Vector Clocks Builder}
In \autoref{alg:havc} we iterate over all nodes in the $\HBG$ in post-order search (which insures that we traverse each node after visiting all of its children) and update $\havc{}{}$ accordingly.
By the end of this algorithm, $\havc{\NodeV}{\Thread} = \HAVC{\NodeV}{\Thread}$ for all nodes $\NodeV \in \Nodes$ and all $\Thread \in \Threads$.

\begin{algorithm}
    \caption{Happens-After Vector Clocks Builder}
    \label{alg:havc}
    \Fn{\FHAVC{$\HBG$}}{
        \BlankLine
        Initialize $\havc{\NodeV}{\Thread} \gets +\infty$ \hfill $\forall \NodeV \in \Nodes, \Thread \in \Threads$ \;
        \BlankLine
        \ForEach{node $\NodeV$ in $\HBG$ in post-order}{
            \label{alg:havc:mainloop}
            $\havc{\NodeV}{\thread{\NodeV}} \gets \idx{\NodeV}$ \label{alg:havc:initialize} \;
            \BlankLine
            \ForEach{parent node $\NodeParent$ such that $(\NodeParent, \NodeV) \in \HBEdges$}{
                \label{alg:havc:parentloop}
                \ForEach{$\Thread \in \Threads$}{
                    $\havc{\NodeParent}{\Thread} \gets \min \left( \havc{\NodeParent}{\Thread}, \havc{\NodeV}{\thread{\NodeV}} \right)$ \label{alg:havc:update} \;
                }
            }
        }
        \BlankLine
        % \KwRet $\havc{\NodeV}{} \hfill \forall \NodeV \in \Nodes$\;
        \KwRet $\havc{}{}$\;
    } 
\end{algorithm}


% ---------------------------------------------------------
% Algorithm - 2. PBVC
% ---------------------------------------------------------

\myparagraph{Persisted-Before Vector Clocks Builder}
In \autoref{alg:pbvc} we iterate over all nodes in the $\HBG$ in a reversed post-order search (which insures that we traverse each node after visiting all of its parents).
We define an auxilary function named $\HBVC{}{}$ that is calculated in $\hbvc{}{}$.
The function $\HBVC{\NodeV}{\Thread,\VarX}$ identifies the index of the last node in thread $\Thread$ concerning variable $\VarX$ that precedes node $\NodeV$.

\begin{algorithm}
    \caption{Persisted-Before Vector Clocks Builder}
    \label{alg:pbvc}
    \Fn{\FPBVC{$\HBG$}}{
        \BlankLine
        Initialize $\hbvc{\NodeV}{\Thread, \VarX} \gets -\infty$ \hfill $\forall \NodeV \in \Nodes, \Thread \in \Threads, \VarX \in \Vars$ \;
        Initialize $\pbvc{\NodeV}{\Thread, \VarX} \gets -\infty$ \hfill $\forall \NodeV \in \Nodes, \Thread \in \Threads, \VarX \in \Vars$ \;
        \BlankLine
        \ForEach{node $\NodeV$ in $\HBG$ in reversed post-order}{
        
            % RW
            \BlankLine
            \Comment*[l]{Handle Read and Write nodes}
            \If{$\NodeV \in \ReadNodes \cup \WriteNodes$}{
                $\hbvc{\NodeV}{\thread{\NodeV},\var{\NodeV}} \gets \idx{\NodeV}$\;
            }
        
            % F
            \BlankLine
            \Comment*[l]{Handle Flush nodes}
            \If{$\NodeV \in \FlushNodes$}{
                \ForEach{$\Thread \in \Threads$}{
                    $\pbvc{\NodeV}{\Thread,\var{\NodeV}} \gets \hbvc{\NodeV}{\Thread,\var{\NodeV}}$\;
                }
            }
        
            % Pass to children
            \BlankLine
            \Comment*[l]{Handle children nodes}
            \ForEach{child node $\NodeChild$ such that $(\NodeV, \NodeChild) \in \HBEdges$}{
                \ForEach{$\Thread \in \Threads$}{
                    \ForEach{$\VarY \in {\Vars}$}{
                        $\hbvc{\NodeChild}{\Thread,\VarY} \gets \max \left( \hbvc{\NodeChild}{\Thread,\VarY}, \hbvc{\NodeV}{\Thread,\VarY} \right)$\;
                        $\pbvc{\NodeChild}{\Thread,\VarY} \gets \max \left( \pbvc{\NodeChild}{\Thread,\VarY}, \pbvc{\NodeV}{\Thread,\VarY} \right)$\;
                    }
                }
            }
        }
        \BlankLine
        % \KwRet $\pbvc{\NodeV}{} \hfill \forall \NodeV \in \Nodes$\;
        \KwRet $\pbvc{}{}$\;
    }
\end{algorithm}


% ---------------------------------------------------------
% Algorithm - 3. CPRD
% ---------------------------------------------------------

\myparagraph{Concurrent Persistence Race Detector}
In \autoref{alg:cprd} we use the previously built $\HAVC{}{}$ and $\PBVC{}{}$ to find the {\racerange}s of each write event node.
For each write node $\NodeWEx \in \WriteNodes$ we look at the dependent read node $\NodeR \in \ReadNodes$
% TODO

\begin{algorithm}
    \caption{Concurrent Persistence Race Detector}
    \label{alg:cprd}
    % \newcommand{\DependentNodeVar}{\NodeV}
    \newcommand{\DependentNodeVar}{\NodeWEx}
    \Fn{\FCPRD{$\HBG$, $\PDG$}}{
        $\havc{}{} \gets \FHAVC{\HBG}$\;
        $\pbvc{}{} \gets \FPBVC{\HBG}$\;
        $\races \gets \EmptySet$\;
        \BlankLine
        \ForEach{write node $\DependentNodeVar \in \WriteNodes$ in $\HBG$}{
            \ForEach{read node $\NodeR \in \ReadNodes$ such that $\PD{\DependentNodeVar}{\NodeR}$ in $\PDG$}{
                % TODO: explain that t wasn't changed or something
                \Assert{$\thread{\NodeR} = \thread{\DependentNodeVar}$} \; % \Comment*[r]{The 2 nodes are on the same thread}
                
                \BlankLine
                \Comment*[l]{If the dependent node and the read node are of the same variable, there is not bug}
                \If{$\var{\DependentNodeVar} = \var{\NodeR}$}{
                    \Continue\;
                }
                
                \BlankLine
                \Comment*[l]{If the read node is persisted, there is not bug}
                \If{$\idx{\NodeR} \leq \pbvc{\DependentNodeVar}{\thread{\DependentNodeVar},\var{\NodeR}}$}{
                    \Continue\;
                }

                \BlankLine
                \ForEach{write node $\NodeW \in \WriteNodes$ such that $\var{\NodeW} = \var{\NodeR}$}{
                    \BlankLine
                    \If{$\pbvc{\DependentNodeVar}{\thread{\NodeW},\var{\NodeW}} < \idx{\NodeW} < \havc{\NodeR}{\thread{\NodeW}}$}{
                        $\races \gets \races \cup {(\NodeR, \NodeW)}$\;
                    }
                }
            }
        
        }
        \BlankLine
        \KwRet $\races$\;
    }
\end{algorithm}


% ---------------------------------------------------------
% Algorithm - Improvments
% ---------------------------------------------------------

\subsection{Algorithm Improvements}

In practice, we combine the 2 last algorithm to one iteration over all the nodes. This is acceptable since when reaching a node $\NodeV$ we already know $\HAVC{\NodeV}{}$ and $\PBVC{\NodeV}{}$.

In addition, 

\subsection{Correctness}
\subsection{Complexity}

Note that the maximum amount of parents/children is $\abs{\Threads}$ since we only allow one happens-before relation from each node to each thread.
We will also assume that practically, $\abs{\Threads} << \abs{\Nodes}$

Part 1 - $\bigO{\abs{\Nodes}\cdot\abs{\Threads}^2} \approx \bigO{\abs{\Nodes}}$

Part 2 - $\bigO{\abs{\Nodes}\cdot\left(\abs{\Threads} + \abs{\Threads}^2\cdot\abs{\Vars} \right)} \approx \bigO{\abs{\Nodes}\cdot\abs{\Vars}} \approx \bigO{\abs{\Nodes}^2}$

Part 3

Since each read node has only one possible edge or less (since we keep only the edge to the first write in that thread), $\abs{\PDEdges} \leq \abs{\ReadNodes}$.

In \autoref{alg:cprd} we effectively iterate over all edges in $\PDEdges$. So, 
$\bigO{\abs{\PDEdges}\cdot\abs{\WriteNodes}} \approx \bigO{\abs{\Nodes}^2}$


In practice, we store linked lists in each thread between event nodes that affect the same cache line. This allows us to quickly iterate over the possible violating write nodes in the \racerange.

The most important optimization is that of dirty cache lines bookkeeping.
We maintain in each thread a set of all cache lines that were operated by the event nodes (written to, read from or flushed)
Empirically this reduces the amount of vector updates, since we only update vectors that there is high chance that were updated since the last merge....
% TODO: this needs more explnation, also, place in the optimization/imporovments subsection. Can we show some complexity calculation here?


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Implementation
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Implementation}

% TODO: add image of full system

\subsection{Program Instrumentation}
TODO: TSAN
TODO: DFSAN


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Evaluation
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Evaluation}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Related Work}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Related Work}
TODO: intro

\textbf{Persistent Memory Bugs Detectors:}
Like PMRACE \cite{pmrace2022}, which focuses on finding concurrent bugs. 
Others like Yashme \cite{yashme2022}, Witcher \cite{witcher2021}, PMDebugger \cite{pmdebugger2021}, XFDetector \cite{xfdetector2020}, PMTest \cite{pmtest2019}















The concept of operating systems for coffee machines is not entirely novel. Previous attempts have focused on various aspects of coffee preparation and automation. Here, we provide a brief overview of these endeavors and highlight how ESPLOS improves upon them.



\textbf{PMRace} \cite{pmrace2022} – a NVM bug detector designed for concurrent NVM programs.
Until recently, most works didn’t focus on NVM bugs in concurrent programs, mostly due to the complex search space that thread scheduling introduces.
In ASPLOS 2022, Chen, Zhangyu, et al. introduced PMRace, that focuses on bug detection in concurrent NVM programs. 
PMRace is a fuzzing-based bug detector that is aimed at detecting 2 types of PM bugs:
\begin{itemize}
    \item PM inter-thread inconsistencies – a program execution in which one thread makes durable side effects based on non-persisted data written by another thread. This is the definition of our concurrent persistency race.
    \item PM synchronization inconsistencies - an inconsistency between a persistent synchronization variable (e.g., a lock, a mutex, etc.) and the non-persistent thread state.
\end{itemize}
PMRace detects PM inter-thread inconsistencies by finding all pairs of read/write operations to the same PM addresses (named PM alias pairs). It later simulates a delay after the write operation that allows the read operation to continue and to hopefully introduce the inconsistency bug.

PMRace tries to maximize its coverage of PM alias pairs to detect more bugs. To find the inconsistencies, PMRace detects program dependency relations and sees if an NVM location, that is dependent on the PM alias pair’s data, is modified with un-persisted data.

\paragraph{Witcher} \cite{witcher2021} – a crash consistency bug detector for NVM software that focuses on exploring the NVM state space.
Witcher uses an LLVM pass to trace the program's execution and to detect possible violations of NVM state. It finds likely invariants on the program’s persistence order and tests its correctness by simulating a crash. Later it checks the equivalence of the NVM state with and without the simulated crash and finds NVM violations if the states are different.
Witcher is designed to be scalable (doesn’t suffer from test space explosion), automatic (doesn’t require manual source code annotations), and precise (doesn’t produce false positives).
Witcher doesn’t focus on exploring thread interleaving, and therefore isn’t suited to find difficult concurrency NVM bugs. They leave this for future work.

\paragraph{VSV-D} (Vector Space Verification – Durability) \cite{vsvd2021} – a dynamic analysis tool that checks durable linearizability at runtime.
Requires user annotations – granularity of operations is defined by the user.
Mostly verifies that all required NVM data is guaranteed to be flushed.

\paragraph{XFDetector} (Cross-Failure Detector) \cite{xfdetector2020}
Detects NVM bugs between the pre-failure and post-failure stages, named Cross-Failure Bugs.
These bugs can be categorized into 2 classes:
\begin{itemize}
    \item Cross-failure races – a post-failure operation that relies on data that wasn’t persisted in the pre-failure stage.
    \item Cross-failure semantic bugs – a post-failure operation that reads data that might be persistent but is considered invalid by program semantics.
\end{itemize}

\paragraph{PMTest} \cite{pmtest2019}
Supplies 2 low-level program checkers:
\begin{itemize}
\item isPersist – checks if an NVM memory location is persisted.
\item isOrderedBefore – checks if an NVM memory location is persisted before another location.
\end{itemize}

Using these checkers, the user can assert program invariants and detect inconsistency bugs.

\textbf{Recipe} \cite{recipe2019} an approach for converting concurrent DRAM data structures into crash consistent data structures for persistent memory.

The common conversion step is to insert a cache line flush and memory fence instructions after each store. 
This approach can produce a bug where data that is modified in one thread can be read by another thread before being persisted. This data can later be used to update other NVM locations that might get persisted before the original data persists. If a crash occurs before the original data persists but after the dependent data persists in another thread, an inconsistency bug will occur. The NVM state after the crash is no longer consistent.

In this work we call such bug a Concurrent Persistency Race, and this is the bug that we hope to detect with our bug detector.



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Conclusions
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusions}
\lipsum[1][1-3]


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Bibliography
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\bibliographystyle{unsrt}
\bibliography{references}

\end{document}
