# TODOs
client script
    a script that will run when docker starts, allows to compile cprd in debug, allows to run benchmarks
    a remote port connection?
    a web view?
dfsan
    abilist to be builtin the instrumentation and not in the benchmarks
    compile cpplib?
DF
    check correctness
    play with "live" parameter
pmdk
    compile pmdk for instumentation
pyprd
    parallize the chain traversals
    refactor code to support only new log file
    logger
cprd
    better log file (serialize)
benchmarks
    pmemcache - add this benchmark?
    cadidates analysis
        compare candidates count after using PD
        calculate how many candidates do I have???
HB
    if data race - make atomic
        (won't do)

