add_llvm_pass_plugin(PrdPass
    # List your source files here.
    # Prd.cpp
    tsanpass.cpp
)

# install(TARGETS ${name} RUNTIME DESTINATION examples)
# set_target_properties(${name} PROPERTIES FOLDER "Examples")
